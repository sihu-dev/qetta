# FORGE LABS Workflows Architecture

## 패키지 구조

```
packages/workflows/
├── package.json                 # 패키지 메타데이터
├── tsconfig.json                # TypeScript 설정
├── README.md                    # 사용 가이드
├── WORKFLOWS.md                 # 아키텍처 문서 (this)
└── src/
    ├── index.ts                 # 메인 엔트리포인트
    ├── types.ts                 # n8n 워크플로우 타입 정의
    ├── manager.ts               # 워크플로우 매니저 (등록/실행/모니터링)
    ├── definitions/             # 워크플로우 정의 (5개)
    │   ├── index.ts
    │   ├── lead-enrichment.ts   # 리드 데이터 강화
    │   ├── lead-scoring.ts      # AI 리드 스코어링
    │   ├── outreach-sequence.ts # 아웃바운드 시퀀스
    │   ├── crm-sync.ts          # CRM 양방향 동기화
    │   └── cross-sell.ts        # 크로스셀 자동화
    └── triggers/                # 트리거 핸들러 (3종)
        ├── index.ts
        ├── webhook.ts           # 웹훅 트리거
        ├── schedule.ts          # 스케줄 트리거 (Cron)
        └── event.ts             # 이벤트 트리거
```

## 워크플로우 상세

### 1. Lead Enrichment (리드 데이터 강화)

**목적**: 웹훅으로 수신한 기본 리드 정보를 AI로 강화하여 DB 저장

**트리거**: Webhook (`/webhook/lead-enrichment`)

**워크플로우 흐름**:

```
┌─────────────────┐
│ Webhook Trigger │ (POST /webhook/lead-enrichment)
└────────┬────────┘
         │ { company_name, contact_email, ... }
         ▼
┌─────────────────────┐
│ Validate Lead Data  │ (필수 필드 검증, 이메일 형식 확인)
└────────┬────────────┘
         │ Normalized data
         ▼
┌─────────────────────┐
│ Enrich Company Info │ (OpenAI GPT-4o-mini)
│                     │ • company_size (startup~enterprise)
│                     │ • estimated_employees
│                     │ • industry_category
│                     │ • technology_stack
│                     │ • business_model (B2B/B2C/B2B2C)
│                     │ • annual_revenue_estimate
│                     │ • growth_stage
│                     │ • key_products
│                     │ • competitive_advantages
└────────┬────────────┘
         │ AI response (JSON)
         ▼
┌──────────────────┐
│ Parse AI Response│ (JSON 파싱, 에러 핸들링)
└────────┬─────────┘
         │ Merged lead + enrichment
         ▼
┌─────────────────┐
│ Save to Database│ (Supabase upsert)
└────────┬────────┘
         │ Saved lead
         ▼
┌─────────────────┐
│ Success Response│ { status, lead_id, enrichment_data }
└─────────────────┘
```

**노드 수**: 6개
**평균 실행 시간**: 2-5초
**에러 핸들링**:
- 필수 필드 누락 → 400 에러
- AI 파싱 실패 → 기본값으로 저장
- DB 저장 실패 → 500 에러

---

### 2. Lead Scoring (AI 리드 스코어링)

**목적**: 미평가 리드를 AI로 자동 스코어링하여 영업 우선순위 결정

**트리거**: Schedule (매 시간, Cron: `0 * * * *`)

**워크플로우 흐름**:

```
┌─────────────────┐
│ Schedule Trigger│ (Every hour)
└────────┬────────┘
         ▼
┌──────────────────────┐
│ Fetch Unscored Leads │ (status='enriched', score IS NULL)
│                      │ LIMIT 50, ORDER BY created_at DESC
└────────┬─────────────┘
         │ [lead1, lead2, ...]
         ▼
┌──────────────────┐
│ AI Lead Scoring  │ (Claude 3.5 Haiku)
│                  │
│ 평가 기준:
│ 1. Company Fit (0-30)
│    - Industry relevance (건설/제조/IT 서비스 = high)
│    - Company size (중소기업 = high)
│    - Business model (B2B, B2B2C = high)
│
│ 2. Buying Intent (0-30)
│    - Website quality
│    - Contact role (대표/실무책임자 = high)
│    - Inquiry detail
│
│ 3. Financial Capacity (0-20)
│    - Revenue estimate
│    - Growth stage (growth/mature = high)
│
│ 4. Engagement Potential (0-20)
│    - Response likelihood
│    - Technology adoption
│
│ 출력:
│ {
│   score: 0-100,
│   tier: "S" | "A" | "B" | "C" | "D",
│   reasoning: string[],
│   recommended_actions: string[],
│   red_flags: string[],
│   estimated_close_probability: 0-100
│ }
└────────┬─────────┘
         │ AI scoring result
         ▼
┌──────────────────┐
│ Parse Score Data │ (검증 & 병합)
└────────┬─────────┘
         │ { id, score, tier, reasoning, ... }
         ▼
┌───────────────────┐
│ Update Lead Score │ (Supabase UPDATE)
└────────┬──────────┘
         │ Updated lead
         ▼
┌──────────────────────┐
│ Filter High Score    │ IF tier IN ['S', 'A']
└────────┬─────────────┘
         │ High-value leads only
         ▼
┌──────────────────────┐
│ Notify High-Value    │ (Slack #qetta-leads)
│ Lead (Slack)         │ "🎯 고득점 리드 발견!"
└──────────────────────┘
```

**Tier 기준**:
- **S Tier (90-100)**: Ideal customer (즉시 연락)
- **A Tier (70-89)**: Strong fit (우선 연락)
- **B Tier (50-69)**: Moderate fit (일반 시퀀스)
- **C Tier (30-49)**: Low fit (낮은 우선순위)
- **D Tier (0-29)**: Poor fit (무시 또는 자동 너처링)

**노드 수**: 7개
**평균 실행 시간**: 5-15초 (배치 50개 기준)
**알림 조건**: Tier S 또는 A만 Slack 알림

---

### 3. Outreach Sequence (아웃바운드 시퀀스)

**목적**: 3단계 이메일 시퀀스로 리드 육성

**트리거**: Schedule (매일 오전 9시, Cron: `0 9 * * *`)

**워크플로우 흐름**:

```
┌─────────────────┐
│ Daily 9AM Trigger│
└────────┬────────┘
         ▼
┌───────────────────────┐
│ Fetch Follow-up Leads │ (score_tier IN ['S','A','B'])
│                       │ (outreach_status='pending')
│                       │ (last_contacted_at IS NULL OR >= 3 days ago)
└────────┬──────────────┘
         │ [lead1, lead2, ...]
         ▼
┌────────────────────────┐
│ Determine Sequence Step│
│                        │
│ Step 1 (초기 접촉):
│ - last_contacted_at IS NULL
│ - 이메일: "Qetta 소개"
│
│ Step 2 (2차 팔로우업):
│ - 3일 경과
│ - 이메일: "추가 정보 + 고객 성과"
│
│ Step 3 (최종 제안):
│ - 7일 경과
│ - 이메일: "특별 혜택 + 마지막 안내"
└────────┬───────────────┘
         │ { sequence_step, email_subject, email_body }
         ▼
┌─────────────┐
│ Send Email  │ (Gmail OAuth2)
│             │ BCC: sales@forge-labs.io
└────────┬────┘
         │ Email sent
         ▼
┌───────────────────────┐
│ Update Outreach Status│ (last_contacted_at = NOW)
└────────┬──────────────┘
         │ Updated lead
         ▼
┌─────────────────────┐
│ Log Outreach Activity│ (qetta_outreach_log)
└─────────────────────┘
```

**이메일 템플릿**:

**Step 1 (초기 접촉)**:
```
제목: {company_name}님, Qetta로 입찰 자동화 하세요

안녕하세요 {contact_name}님,

Qetta는 공공입찰/조달 프로세스를 자동화하는 AI 플랫폼입니다.
귀사({company_name})와 같은 {industry}에서 평균 60% 이상의 시간 절감 효과를 보고 있습니다.

주요 기능:
• 나라장터/지자체 입찰 공고 자동 매칭
• AI 기반 제안서 초안 작성
• 입찰 마감일 알림 및 관리

15분 데모 미팅으로 귀사에 맞는 활용법을 안내드리겠습니다.
```

**Step 2 (2차 팔로우업, 3일 후)**:
```
제목: Re: {company_name}님, Qetta 데모 제안

최근 유사 업종 고객사 성과:
• 월 평균 30개 입찰 기회 발굴
• 제안서 작성 시간 70% 단축
• 낙찰률 25% 향상
```

**Step 3 (최종 제안, 7일 후)**:
```
제목: 마지막 안내: Qetta 특별 혜택

이번 달 한정으로 초기 도입 고객사 대상
3개월 무료 체험 프로그램을 운영 중입니다.

관심 없으시면 더 이상 연락드리지 않겠습니다.
```

**노드 수**: 6개
**평균 실행 시간**: 1-3초 (리드당)
**시퀀스 중단 조건**: Step 3 이후 또는 리드 상태 변경 시

---

### 4. CRM Sync (CRM 양방향 동기화)

**목적**: Qetta ↔ HubSpot 데이터 실시간 동기화

**트리거**: Schedule (15분마다, Cron: `*/15 * * * *`)

**워크플로우 흐름 (양방향)**:

```
┌──────────────────┐
│ Every 15min Sync │
└────────┬─────────┘
         │
         ├─────────────────────┬─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────┐   ┌──────────────────┐   (병렬 실행)
│ Qetta → HubSpot│   │ HubSpot → Qetta│
└────────┬────────┘   └────────┬─────────┘
         │                     │
         ▼                     ▼
┌─────────────────────┐   ┌──────────────────────┐
│ Fetch Qetta Leads │   │ Fetch HubSpot Contacts│
│ (crm_synced_at IS   │   │ (createdate >= -15min)│
│  NULL OR            │   │ (qetta_lead_id IS   │
│  updated_at >= -15m)│   │  NULL)                │
└────────┬────────────┘   └────────┬──────────────┘
         │                         │
         ▼                         ▼
┌─────────────────────┐   ┌──────────────────────┐
│ Transform to HubSpot│   │ Transform to Qetta │
│                     │   │                      │
│ Qetta → HubSpot:  │   │ HubSpot → Qetta:   │
│ {                   │   │ {                    │
│   email             │   │   company_name       │
│   firstname         │   │   contact_email      │
│   lastname          │   │   contact_name       │
│   company           │   │   phone              │
│   phone             │   │   website            │
│   website           │   │   industry           │
│   industry          │   │   source: 'HubSpot'  │
│   lifecyclestage    │   │   crm_type           │
│   hs_lead_status    │   │   crm_contact_id     │
│   qetta_lead_id   │   │ }                    │
│   qetta_score     │   │                      │
│   qetta_tier      │   │                      │
│   ...custom fields  │   │                      │
│ }                   │   │                      │
└────────┬────────────┘   └────────┬─────────────┘
         │                         │
         ▼                         ▼
┌─────────────────────┐   ┌──────────────────────┐
│ Upsert to HubSpot   │   │ Save to Qetta      │
│ (by email)          │   │ (upsert by email)    │
└────────┬────────────┘   └────────┬─────────────┘
         │                         │
         ▼                         ▼
┌─────────────────────┐   ┌──────────────────────┐
│ Update Sync Status  │   │                      │
│ (crm_synced_at,     │   │                      │
│  crm_contact_id)    │   │                      │
└─────────────────────┘   └──────────────────────┘
```

**동기화 매핑**:

| Qetta Field             | HubSpot Field           | Direction |
|---------------------------|-------------------------|-----------|
| contact_email             | email                   | ↔         |
| contact_name              | firstname + lastname    | ↔         |
| company_name              | company                 | ↔         |
| phone                     | phone                   | ↔         |
| website                   | website                 | ↔         |
| industry                  | industry                | ↔         |
| score                     | qetta_score (custom)  | →         |
| score_tier                | qetta_tier (custom)   | →         |
| enrichment.company_size   | company_size (custom)   | →         |
| enrichment.business_model | business_model (custom) | →         |
| created_at                | createdate              | ↔         |
| updated_at                | lastmodifieddate        | ↔         |

**노드 수**: 8개 (양방향 각 4개)
**평균 실행 시간**: 5-10초 (배치 100개 기준)
**충돌 해결**:
- Email 기준 Upsert (중복 방지)
- 최신 updated_at 우선

---

### 5. Cross-Sell (크로스셀 자동화)

**목적**: Qetta ↔ HEPHAITOS 간 크로스셀 제안

**트리거**: Schedule (매일 오후 3시, Cron: `0 15 * * *`)

**워크플로우 흐름 (양방향)**:

```
┌─────────────────┐
│ Daily 3PM Trigger│
└────────┬────────┘
         │
         ├─────────────────────┬─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
┌───────────────────┐   ┌───────────────────────┐
│ Qetta → HEPHAITOS│   │ HEPHAITOS → Qetta  │
└────────┬──────────┘   └────────┬──────────────┘
         │                       │
         ▼                       ▼
┌────────────────────────┐   ┌──────────────────────────┐
│ Fetch Qetta Customers│   │ Fetch HEPHAITOS Mentors  │
│ (score_tier='A',       │   │ (user_type='MENTOR',     │
│  status='customer',    │   │  status='ACTIVE',        │
│  cross_sell_offered    │   │  cross_sell_offered IS   │
│  IS NULL)              │   │  NULL)                   │
└────────┬───────────────┘   └────────┬─────────────────┘
         │                           │
         ▼                           ▼
┌────────────────────────┐   ┌──────────────────────────┐
│ Analyze Cross-Sell Fit │   │ Create In-App Message    │
│                        │   │                          │
│ 점수 계산 (0-100):      │   │ "멘토님, Qetta로       │
│                        │   │  추가 수익 창출하세요"   │
│ +30: 금융/투자 업종    │   │                          │
│ +25: 중형 이상 기업    │   │ 특별 혜택:               │
│ +20: 높은 기술 친화도  │   │ • 3개월 무료 체험        │
│ +15: B2B 비즈니스      │   │ • 우선 고객 지원         │
│ +10: 높은 연매출       │   │                          │
│                        │   │ CTA: "Qetta 시작하기"  │
│ 결과:                  │   │ (ref=hephaitos&mentor=ID)│
│ {                      │   │                          │
│   cross_sell_score,    │   │                          │
│   reasons: string[],   │   │                          │
│   recommend: boolean   │   │                          │
│ }                      │   │                          │
└────────┬───────────────┘   └────────┬─────────────────┘
         │                           │
         ▼                           ▼
┌────────────────────────┐   ┌──────────────────────────┐
│ Filter High Score      │   │ Save In-App Message      │
│ (recommend=true,       │   │ (hephaitos_notifications)│
│  score >= 50)          │   │                          │
└────────┬───────────────┘   └────────┬─────────────────┘
         │                           │
         ▼                           ▼
┌────────────────────────┐   ┌──────────────────────────┐
│ Generate Cross-Sell    │   │ Log Cross-Sell           │
│ Email (GPT-4o-mini)    │   │ (cross_sell_offered=     │
│                        │   │  'qetta',              │
│ Personalized for:      │   │  offered_at = NOW)       │
│ - Company              │   │                          │
│ - Industry             │   │                          │
│ - Business model       │   │                          │
│                        │   │                          │
│ Highlight:             │   │                          │
│ 1. 수익원 다각화       │   │                          │
│ 2. 자동화 시너지       │   │                          │
│ 3. 20% 할인            │   │                          │
└────────┬───────────────┘   └──────────────────────────┘
         │
         ▼
┌────────────────────────┐
│ Send Cross-Sell Email  │ (Gmail)
└────────┬───────────────┘
         │
         ▼
┌────────────────────────┐
│ Log Cross-Sell Offer   │ (cross_sell_offered='hephaitos')
└────────────────────────┘
```

**크로스셀 적합성 점수**:

| 조건                        | 점수 | 이유                           |
|-----------------------------|------|--------------------------------|
| 금융/투자 업종              | +30  | HEPHAITOS 타겟 업종            |
| 중형 이상 기업              | +25  | 투자 여력 높음                 |
| 높은 기술 친화도 (stack>3)  | +20  | 신규 플랫폼 도입 용이          |
| B2B/B2B2C 비즈니스          | +15  | HEPHAITOS 적합 모델            |
| 높은 연매출 (M/억 단위)     | +10  | 구매력 높음                    |

**추천 기준**: `cross_sell_score >= 50`

**노드 수**: 11개 (양방향 각 5-6개)
**평균 실행 시간**: 10-30초 (AI 이메일 생성 포함)
**전환 추적**: `cross_sell_accepted` 필드로 전환율 측정

---

## 트리거 시스템

### 1. Webhook Trigger

**지원 기능**:
- HTTP 메서드: GET, POST, PUT, DELETE, PATCH
- 인증: none, basicAuth, headerAuth
- 응답 모드: onReceived, lastNode, responseNode

**예시**:
```typescript
// 웹훅 URL 생성
const url = generateWebhookUrl(
  'https://api.forge-labs.io',
  'lead-enrichment',
  'webhook-trigger'
);
// → https://api.forge-labs.io/webhook/lead-enrichment/webhook-trigger

// HMAC 서명 검증
const isValid = await verifyHmacSignature(payload, signature, secret);
```

### 2. Schedule Trigger (Cron)

**일반적인 패턴**:

| 패턴              | Cron 표현식   | 설명                |
|-------------------|---------------|---------------------|
| EVERY_MINUTE      | `* * * * *`   | 매 분마다           |
| EVERY_5_MINUTES   | `*/5 * * * *` | 5분마다             |
| EVERY_15_MINUTES  | `*/15 * * * *`| 15분마다            |
| EVERY_HOUR        | `0 * * * *`   | 매 시간             |
| DAILY_9AM         | `0 9 * * *`   | 매일 오전 9시       |
| WEEKLY_MONDAY_9AM | `0 9 * * 1`   | 매주 월요일 오전 9시|

**타임존 지원**: `Asia/Seoul`, `UTC`, `America/New_York`, etc.

### 3. Event Trigger

**일반적인 이벤트**:

| 이벤트                | 패턴                  | 설명                |
|-----------------------|-----------------------|---------------------|
| lead.created          | `lead.created`        | 리드 생성           |
| lead.scored           | `lead.scored`         | 리드 스코어링 완료  |
| lead.converted        | `lead.converted`      | 리드 전환           |
| outreach.email.opened | `outreach.email.*`    | 이메일 오픈/클릭    |
| crm.contact.created   | `crm.*`               | CRM 이벤트          |
| cross_sell.accepted   | `cross_sell.accepted` | 크로스셀 성공       |

**필터 예시**:
```typescript
// Tier S,A 리드만
const filter = {
  score_tier: ['S', 'A'],
};

// 점수 70 이상
const filter = {
  score: { $gte: 70 },
};

// 복합 조건
const filter = {
  score: { $gte: 60 },
  status: 'enriched',
  'enrichment.company_size': { $in: ['medium', 'large', 'enterprise'] },
};
```

---

## 워크플로우 매니저 API

### 등록 및 실행

```typescript
import { WorkflowManager, ALL_WORKFLOWS } from '@forge/workflows';

const manager = new WorkflowManager({
  maxConcurrentExecutions: 10,
  executionTimeout: 300000, // 5분
  retryAttempts: 3,
  retryDelay: 5000,
});

// 일괄 등록
manager.registerWorkflows(ALL_WORKFLOWS);

// 수동 실행
const result = await manager.executeWorkflow(
  'lead-enrichment-v1',
  'manual',
  { company_name: 'ABC Corp', contact_email: 'contact@abc.com' }
);

console.log(result);
// {
//   execution_id: 'exec_1234567890_abc',
//   workflow_id: 'lead-enrichment-v1',
//   status: 'success',
//   data: { ... },
//   duration_ms: 2340,
//   nodes_executed: 6,
//   timestamp: '2025-12-24T...'
// }
```

### 모니터링 및 통계

```typescript
// 통계
const stats = manager.getStats();
console.log(stats);
// {
//   total_workflows: 5,
//   active_workflows: 5,
//   total_executions: 234,
//   running_executions: 2,
//   success_rate: 98.5
// }

// 실행 이력
const history = manager.getWorkflowExecutions('lead-enrichment-v1', 10);

// 특정 실행 조회
const execution = manager.getExecution('exec_1234567890_abc');

// 재시도
await manager.retryWorkflow(executionId);

// 취소
await manager.cancelWorkflow(executionId);
```

---

## 성능 지표

| 워크플로우             | 평균 실행 시간 | 노드 수 | 배치 크기 | 권장 빈도       |
|------------------------|----------------|---------|-----------|-----------------|
| Lead Enrichment        | 2-5초          | 6       | 1         | 실시간 (Webhook)|
| Lead Scoring           | 5-15초         | 7       | 50        | 매 시간         |
| Outreach Sequence      | 1-3초/리드     | 6       | 100       | 매일 오전 9시   |
| CRM Sync               | 5-10초         | 8       | 100       | 15분마다        |
| Cross-Sell             | 10-30초        | 11      | 50        | 매일 오후 3시   |

---

## 에러 핸들링

### 재시도 전략

```typescript
const config = {
  retryAttempts: 3,
  retryDelay: 5000, // 5초
};

// 자동 재시도: 네트워크 에러, 타임아웃, 일시적 API 에러
// 재시도 안함: 검증 에러, 인증 에러, 데이터 에러
```

### 에러 알림

- **Slack**: 워크플로우 실행 실패 시 `#qetta-alerts` 채널 알림
- **이메일**: 연속 3회 실패 시 관리자 이메일 발송
- **로깅**: Supabase에 전체 실행 로그 저장

---

## 배포 가이드

### n8n 서버 설정

```bash
# Docker Compose
docker-compose up -d n8n

# 환경 변수
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=***
N8N_ENCRYPTION_KEY=***
WEBHOOK_URL=https://api.forge-labs.io/
```

### 워크플로우 임포트

```bash
# JSON 파일 생성
npm run build:workflows

# n8n CLI로 임포트
n8n import:workflow --input=./workflows/lead-enrichment.json
```

---

## 로드맵

### Phase 1 (현재)
- ✅ 5개 핵심 워크플로우 구현
- ✅ 3종 트리거 지원 (Webhook, Schedule, Event)
- ✅ 워크플로우 매니저 구현

### Phase 2 (Q1 2026)
- [ ] 실시간 워크플로우 모니터링 대시보드
- [ ] A/B 테스팅 (이메일 템플릿, 시퀀스 타이밍)
- [ ] 워크플로우 버전 관리
- [ ] 조건부 분기 고도화

### Phase 3 (Q2 2026)
- [ ] 커스텀 노드 개발 (Qetta 전용)
- [ ] 워크플로우 템플릿 마켓플레이스
- [ ] Multi-tenant 지원
- [ ] 실시간 협업 편집

---

**FORGE LABS** - Automating the future, one workflow at a time.
