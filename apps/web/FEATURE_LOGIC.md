# BIDFLOW 기능 로직 상세 가이드

> **버전**: v0.1.0
> **업데이트**: 2025-12-21
> **대상**: 개발자, 시스템 분석가

---

## 📋 목차

1. [핵심 기능 개요](#핵심-기능-개요)
2. [매칭 엔진 알고리즘](#매칭-엔진-알고리즘)
3. [AI 스마트 함수](#ai-스마트-함수)
4. [크롤링 워크플로우](#크롤링-워크플로우)
5. [제안서 생성 로직](#제안서-생성-로직)
6. [데이터 처리 파이프라인](#데이터-처리-파이프라인)

---

## 핵심 기능 개요

### 시스템 전체 흐름도

```
┌───────────────────────────────────────────────────────────────┐
│ 1. 데이터 수집 (Crawling)                                      │
│    45+ 데이터 소스 → 입찰 공고 수집 → 정규화 → DB 저장        │
└─────────────────────────┬─────────────────────────────────────┘
                          │
┌─────────────────────────▼─────────────────────────────────────┐
│ 2. 자동 매칭 (Enhanced Matcher)                                │
│    입찰 공고 → 키워드/규격/기관 분석 → 5개 제품 매칭          │
└─────────────────────────┬─────────────────────────────────────┘
                          │
┌─────────────────────────▼─────────────────────────────────────┐
│ 3. AI 분석 (Smart Functions)                                   │
│    AI_SUMMARY(), AI_SCORE(), AI_MATCH() → 요약/점수/추천       │
└─────────────────────────┬─────────────────────────────────────┘
                          │
┌─────────────────────────▼─────────────────────────────────────┐
│ 4. 사용자 리뷰 (Dashboard)                                     │
│    매칭 결과 확인 → BID/REVIEW/SKIP 결정                       │
└─────────────────────────┬─────────────────────────────────────┘
                          │
┌─────────────────────────▼─────────────────────────────────────┐
│ 5. 제안서 생성 (AI Generator)                                  │
│    템플릿 + 공고 정보 + 제품 스펙 → 맞춤형 제안서              │
└───────────────────────────────────────────────────────────────┘
```

---

## 매칭 엔진 알고리즘

### Enhanced Matcher 핵심 로직

**위치**: `src/lib/matching/enhanced-matcher.ts`

#### 1. 매칭 프로세스

```typescript
function matchBidToProducts(bid: BidAnnouncement): MatchingResult {
  const allMatches: MatchResult[] = [];

  // 1단계: 모든 제품에 대해 점수 계산
  for (const product of CMNTECH_PRODUCTS) {
    const match = calculateMatch(bid, product);
    allMatches.push(match);
  }

  // 2단계: 점수 내림차순 정렬
  allMatches.sort((a, b) => b.score - a.score);

  // 3단계: 최고 점수 제품 선택
  const bestMatch = allMatches[0];

  // 4단계: 추천 전략 결정
  const recommendation = determineRecommendation(bestMatch, bid);

  return { bestMatch, allMatches, recommendation };
}
```

#### 2. 점수 계산 로직

**총점 = 키워드 점수 (100) + 규격 점수 (25) + 기관 점수 (50) = 175점**

##### A. 키워드 점수 (100점 만점)

```typescript
function calculateKeywordScore(
  bidText: string,
  product: Product
): { score: number; reasons: string[] } {
  let score = 0;
  const reasons: string[] = [];

  // 1. 제외 키워드 검사 (즉시 0점 처리)
  for (const keyword of product.excludeKeywords) {
    if (bidText.includes(keyword)) {
      return { score: 0, reasons: [`제외 키워드 발견: "${keyword}"`] };
    }
  }

  // 2. 강한 키워드 매칭 (각 20점, 최대 100점)
  for (const keyword of product.strongKeywords) {
    if (bidText.includes(keyword)) {
      score += 20;
      reasons.push(`강한 키워드 매칭: "${keyword}" (+20점)`);
    }
  }

  // 3. 약한 키워드 매칭 (각 5점, 최대 20점)
  let weakScore = 0;
  for (const keyword of product.weakKeywords) {
    if (bidText.includes(keyword) && weakScore < 20) {
      weakScore += 5;
      reasons.push(`약한 키워드 매칭: "${keyword}" (+5점)`);
    }
  }

  score += weakScore;
  return { score: Math.min(score, 100), reasons };
}
```

**예시**:
```
공고: "서울시 상수도본부 초음파 유량계 DN300~DN1000 납품"

UR-1000PLUS 제품:
  - 강한 키워드: "초음파" (+20점), "유량계" (+20점), "상수도" (+20점)
  - 약한 키워드: "납품" (+5점)
  - 합계: 65점
```

##### B. 규격 점수 (25점 만점)

```typescript
function calculatePipeSizeScore(
  bid: BidAnnouncement,
  product: Product
): { score: number; reasons: string[] } {
  // 1. 공고에서 파이프 규격 추출
  const extractedSizes = extractPipeSizes(bid.title + ' ' + bid.description);

  if (extractedSizes.dns.length === 0) {
    return { score: 0, reasons: ['파이프 규격 정보 없음'] };
  }

  // 2. 제품 지원 범위와 비교
  const matchedSizes = extractedSizes.dns.filter(
    dn => dn >= product.pipeSizeRange.min && dn <= product.pipeSizeRange.max
  );

  // 3. 매칭 비율에 따른 점수
  const matchRatio = matchedSizes.length / extractedSizes.dns.length;

  let score = 0;
  if (matchRatio === 1.0) {
    score = 25; // 100% 매칭
  } else if (matchRatio >= 0.7) {
    score = 15; // 70% 이상
  } else if (matchRatio >= 0.5) {
    score = 10; // 50% 이상
  } else {
    score = 5;  // 일부 매칭
  }

  return {
    score,
    reasons: [
      `파이프 규격: ${extractedSizes.dns.join(', ')}`,
      `제품 지원 범위: DN${product.pipeSizeRange.min}~DN${product.pipeSizeRange.max}`,
      `매칭률: ${(matchRatio * 100).toFixed(0)}% (+${score}점)`
    ]
  };
}
```

**예시**:
```
공고: "DN300, DN500, DN800 납품"
제품: UR-1000PLUS (DN300~DN4000)

→ 3/3 매칭 (100%) → 25점
```

##### C. 기관 점수 (50점 만점)

```typescript
function calculateOrganizationScore(
  bid: BidAnnouncement,
  product: Product
): { score: number; reasons: string[] } {
  // 1. 발주기관 정규화
  const normalized = normalizeOrganization(bid.organization);

  if (!normalized.entry) {
    return { score: 0, reasons: ['기관 정보 매칭 실패'] };
  }

  // 2. 해당 기관의 제품별 가중치 확인
  const productScore = normalized.entry.relatedProducts[product.id] || 0;

  // 3. 신뢰도에 따른 보너스
  const confidenceBonus =
    normalized.confidence === 'exact' ? 5 :
    normalized.confidence === 'alias' ? 3 : 0;

  const totalScore = productScore + confidenceBonus;

  return {
    score: totalScore,
    reasons: [
      `발주기관: ${normalized.canonical}`,
      `기관 점수: ${productScore}점`,
      `매칭 신뢰도: ${normalized.confidence} (+${confidenceBonus}점)`
    ]
  };
}
```

**예시**:
```
공고: "한국수자원공사 정수장 유량계 납품"

정규화: "한국수자원공사" (K-water) - exact match
K-water의 UR-1000PLUS 가중치: 45점
신뢰도 보너스: +5점
→ 총 50점
```

#### 3. 신뢰도 계산

```typescript
function calculateConfidence(totalScore: number): 'high' | 'medium' | 'low' {
  if (totalScore >= 80) return 'high';
  if (totalScore >= 60) return 'medium';
  return 'low';
}
```

#### 4. 추천 전략 결정

```typescript
function determineRecommendation(
  match: MatchResult,
  bid: BidAnnouncement
): 'BID' | 'REVIEW' | 'SKIP' {
  // 1. 제외 키워드가 있으면 무조건 SKIP
  if (match.score === 0) {
    return 'SKIP';
  }

  // 2. High 신뢰도 + 갭 없음 → BID
  if (match.confidence === 'high' && (!match.gaps || match.gaps.length === 0)) {
    return 'BID';
  }

  // 3. Medium 이상 → REVIEW
  if (match.confidence === 'medium' || match.confidence === 'high') {
    return 'REVIEW';
  }

  // 4. Low 신뢰도 → SKIP
  return 'SKIP';
}
```

### 매칭 결과 예시

#### 케이스 1: 완벽 매칭 (BID)

```json
{
  "bid": {
    "title": "서울시 상수도본부 초음파 유량계 DN300~DN1000 납품",
    "organization": "서울특별시 상수도사업본부"
  },
  "bestMatch": {
    "productId": "UR-1000PLUS",
    "score": 140,
    "confidence": "high",
    "breakdown": {
      "keywordScore": 65,
      "pipeSizeScore": 25,
      "organizationScore": 50
    },
    "reasons": [
      "강한 키워드 매칭: \"초음파\" (+20점)",
      "강한 키워드 매칭: \"유량계\" (+20점)",
      "강한 키워드 매칭: \"상수도\" (+20점)",
      "파이프 규격 100% 매칭 (+25점)",
      "발주기관: 서울특별시 상수도사업본부 (+50점)"
    ],
    "gaps": []
  },
  "recommendation": "BID"
}
```

#### 케이스 2: 부분 매칭 (REVIEW)

```json
{
  "bid": {
    "title": "한전 발전소 열량계 납품",
    "organization": "한국전력공사"
  },
  "bestMatch": {
    "productId": "EnerRay",
    "score": 70,
    "confidence": "medium",
    "breakdown": {
      "keywordScore": 25,
      "pipeSizeScore": 0,
      "organizationScore": 45
    },
    "reasons": [
      "강한 키워드 매칭: \"열량계\" (+20점)",
      "약한 키워드 매칭: \"납품\" (+5점)",
      "파이프 규격 정보 없음 (0점)",
      "발주기관: 한국전력공사 (+45점)"
    ],
    "gaps": [
      "파이프 규격 정보 부족 - 추가 확인 필요"
    ]
  },
  "recommendation": "REVIEW"
}
```

#### 케이스 3: 제외 (SKIP)

```json
{
  "bid": {
    "title": "부산시 전자식 유량계 납품",
    "organization": "부산광역시"
  },
  "bestMatch": {
    "productId": "UR-1000PLUS",
    "score": 0,
    "confidence": "low",
    "breakdown": {
      "keywordScore": 0,
      "pipeSizeScore": 0,
      "organizationScore": 0
    },
    "reasons": [
      "제외 키워드 발견: \"전자식\" - UR-1000PLUS는 초음파 방식"
    ],
    "gaps": []
  },
  "recommendation": "SKIP"
}
```

---

## AI 스마트 함수

### 5개 AI 함수 로직

**위치**: `src/lib/data/ai-functions.ts`

#### 1. AI_SUMMARY() - 공고 요약

```typescript
/**
 * 입찰 공고를 2-3문장으로 요약
 *
 * 알고리즘:
 * 1. 핵심 정보 추출 (발주기관, 물품명, 수량, 납기)
 * 2. 중복 제거 및 문장 정리
 * 3. 2-3문장으로 압축
 */
function AI_SUMMARY(bidText: string): string {
  // 향후 Claude API 연동 예정
  // 현재는 규칙 기반 요약

  const sentences = bidText.split(/[.!?]/);
  const keyInfo = extractKeyInformation(sentences);

  return `${keyInfo.organization}에서 ${keyInfo.item} ${keyInfo.quantity}를 ${keyInfo.deadline}까지 납품할 입찰을 공고했습니다. 추정가격은 ${keyInfo.price}입니다.`;
}
```

**예시**:
```
입력: "서울특별시 상수도사업본부에서 2025년 상수도 관로 유량계 교체사업을 위한 초음파 유량계 50대를 구매하고자 합니다. DN300~DN1000 규격이며, 납기는 2025년 6월 30일입니다. 추정가격은 450,000,000원입니다..."

출력: "서울특별시 상수도사업본부에서 초음파 유량계 50대를 2025년 6월 30일까지 납품할 입찰을 공고했습니다. 추정가격은 4.5억원입니다."
```

#### 2. AI_SCORE() - 낙찰 가능성 점수

```typescript
/**
 * 낙찰 가능성을 0-100% 점수로 계산
 *
 * 알고리즘:
 * 1. 매칭 점수 (60%) - Enhanced Matcher 결과
 * 2. 경쟁 강도 (20%) - 추정가격 대비 시장 경쟁
 * 3. 과거 실적 (20%) - 유사 공고 낙찰 이력
 */
function AI_SCORE(bid: BidAnnouncement, product: Product): number {
  const matchResult = matchBidToProducts(bid);
  const matchScore = matchResult.bestMatch.score / 175; // 0-1 정규화

  const competitionScore = calculateCompetitionIntensity(bid);
  const historyScore = getHistoricalSuccessRate(bid, product);

  const finalScore = (
    matchScore * 0.6 +
    competitionScore * 0.2 +
    historyScore * 0.2
  ) * 100;

  return Math.round(finalScore);
}
```

**예시**:
```
매칭 점수: 140/175 = 0.8 → 80점 (가중치 60% = 48점)
경쟁 강도: 0.7 (중간) → 70점 (가중치 20% = 14점)
과거 실적: 0.9 (높음) → 90점 (가중치 20% = 18점)

최종 점수: 48 + 14 + 18 = 80점
```

#### 3. AI_MATCH() - 최적 제품 추천

```typescript
/**
 * 입찰에 가장 적합한 제품 추천
 *
 * 알고리즘:
 * 1. 모든 제품 매칭 점수 계산
 * 2. 최고 점수 제품 선택
 * 3. 차선책 제품도 함께 제공 (점수 차이 20점 이내)
 */
function AI_MATCH(bid: BidAnnouncement): {
  primary: string;
  alternatives?: string[];
  reason: string;
} {
  const result = matchBidToProducts(bid);
  const bestMatch = result.bestMatch;

  // 차선책 찾기 (점수 차이 20점 이내)
  const alternatives = result.allMatches
    .filter(m => m.score >= bestMatch.score - 20 && m.productId !== bestMatch.productId)
    .map(m => m.productId);

  return {
    primary: bestMatch.productId,
    alternatives: alternatives.length > 0 ? alternatives : undefined,
    reason: bestMatch.reasons[0]
  };
}
```

**예시**:
```
입력: "상수도 초음파 유량계 DN500"

출력:
{
  "primary": "UR-1000PLUS",
  "alternatives": ["MF-1000C"],
  "reason": "초음파 유량계 + 상수도 키워드 완벽 매칭"
}
```

#### 4. AI_KEYWORDS() - 핵심 키워드 추출

```typescript
/**
 * 공고에서 핵심 키워드 3개 추출
 *
 * 알고리즘:
 * 1. TF-IDF 기반 중요도 계산
 * 2. 제품 관련 키워드 우선순위
 * 3. 상위 3개 선택
 */
function AI_KEYWORDS(bidText: string): string[] {
  const words = tokenize(bidText);
  const tfidf = calculateTFIDF(words);

  // 제품 관련 키워드에 가중치 부여
  const productKeywords = ['초음파', '유량계', '전자', '비만관', '개수로', '열량계'];
  const boosted = tfidf.map(item => ({
    ...item,
    score: productKeywords.includes(item.word) ? item.score * 1.5 : item.score
  }));

  return boosted
    .sort((a, b) => b.score - a.score)
    .slice(0, 3)
    .map(item => item.word);
}
```

**예시**:
```
입력: "서울시 상수도본부 초음파 유량계 DN300~DN1000 납품 및 설치"

출력: ["초음파유량계", "상수도", "DN300-1000"]
```

#### 5. AI_DEADLINE() - 마감일 분석

```typescript
/**
 * 마감일 분석 및 권장 액션
 *
 * 알고리즘:
 * 1. D-Day 계산
 * 2. 긴급도 판단 (긴급/보통/여유)
 * 3. 단계별 일정 제안
 */
function AI_DEADLINE(deadline: Date): {
  dday: number;
  urgency: 'urgent' | 'normal' | 'relaxed';
  actions: string[];
} {
  const today = new Date();
  const dday = Math.ceil((deadline.getTime() - today.getTime()) / (1000 * 60 * 60 * 24));

  let urgency: 'urgent' | 'normal' | 'relaxed';
  let actions: string[];

  if (dday <= 3) {
    urgency = 'urgent';
    actions = [
      '즉시 내부 검토 회의',
      '오늘 중 입찰 참여 여부 결정',
      '기존 제안서 템플릿 활용'
    ];
  } else if (dday <= 7) {
    urgency = 'normal';
    actions = [
      'D-7: 내부 검토 및 의사결정',
      'D-3: 제안서 작성 완료',
      'D-1: 최종 검토 및 제출'
    ];
  } else {
    urgency = 'relaxed';
    actions = [
      `D-${dday-7}: 예비 검토`,
      'D-7: 제안서 작성 시작',
      'D-2: 최종 검토 및 제출'
    ];
  }

  return { dday, urgency, actions };
}
```

---

## 크롤링 워크플로우

### 데이터 수집 프로세스 (예정)

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 크롤링 스케줄러 (Inngest Cron Job)                        │
│    - 나라장터: 매일 06:00, 18:00                             │
│    - TED API: 매일 09:00                                     │
│    - K-water: 매일 12:00                                     │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 2. 데이터 수집                                               │
│    - API 호출 또는 HTML 파싱                                 │
│    - 공고 제목, 기관, 금액, 마감일 추출                      │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 3. 정규화 및 중복 제거                                       │
│    - 기관명 정규화 (normalizeOrganization)                   │
│    - 중복 공고 체크 (제목 + 기관 + 마감일 해시)              │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 4. DB 저장                                                   │
│    - Supabase bids 테이블 INSERT                             │
│    - 상태: 'pending'                                         │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 5. 자동 매칭 트리거                                          │
│    - Enhanced Matcher 실행                                   │
│    - matches 테이블에 결과 저장                              │
└─────────────────┬───────────────────────────────────────────┘
                  │
┌─────────────────▼───────────────────────────────────────────┐
│ 6. 알림 발송 (High 신뢰도만)                                 │
│    - 이메일 또는 Slack 알림                                  │
│    - "새로운 입찰 공고 (92% 매칭) 발견"                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 제안서 생성 로직

### AI 제안서 생성 프로세스 (예정)

```typescript
/**
 * 입찰 제안서 자동 생성
 *
 * 입력:
 * - bid: 입찰 공고 정보
 * - product: 매칭된 제품
 * - company: 회사 정보 (씨엠엔텍)
 *
 * 출력:
 * - 워드 문서 또는 PDF
 */
async function generateProposal(
  bid: BidAnnouncement,
  product: Product,
  company: CompanyInfo
): Promise<ProposalDocument> {
  // 1. 템플릿 로드
  const template = await loadProposalTemplate(product.category);

  // 2. Claude API로 커스터마이징
  const prompt = `
    다음 입찰 공고에 대한 제안서를 작성해주세요.

    공고 정보:
    - 제목: ${bid.title}
    - 발주기관: ${bid.organization}
    - 추정가격: ${bid.estimatedPrice}원

    제품 정보:
    - 제품명: ${product.name}
    - 규격: DN${product.pipeSizeRange.min}~DN${product.pipeSizeRange.max}
    - 특징: ${product.features}

    회사 정보:
    - 회사명: ${company.name}
    - 설치 실적: ${company.installCount}대 (${company.yearsInBusiness}년간)

    제안서 구성:
    1. 사업 개요
    2. 제품 사양 및 특장점
    3. 설치 실적
    4. 견적서
    5. 납품 일정
  `;

  const aiResponse = await callClaudeAPI(prompt);

  // 3. 템플릿에 AI 생성 내용 삽입
  const document = mergeTemplateWithAI(template, aiResponse);

  // 4. PDF 변환
  const pdf = await convertToPDF(document);

  return pdf;
}
```

---

## 데이터 처리 파이프라인

### 실시간 처리 흐름

```
사용자 액션: "입찰 공고 조회"
         │
         ▼
┌────────────────────────┐
│ 1. API 요청 수신        │
│ GET /api/v1/bids       │
└───────┬────────────────┘
        │
        ▼
┌────────────────────────┐
│ 2. 인증 & 권한 체크     │
│ - JWT 검증             │
│ - Rate Limit 확인      │
└───────┬────────────────┘
        │
        ▼
┌────────────────────────┐
│ 3. Supabase 쿼리       │
│ - bids 테이블 SELECT   │
│ - 필터/정렬 적용       │
└───────┬────────────────┘
        │
        ▼
┌────────────────────────┐
│ 4. 매칭 결과 조인       │
│ - matches 테이블 JOIN  │
│ - 제품 정보 포함       │
└───────┬────────────────┘
        │
        ▼
┌────────────────────────┐
│ 5. AI 함수 실행         │
│ - AI_SUMMARY()         │
│ - AI_SCORE()           │
└───────┬────────────────┘
        │
        ▼
┌────────────────────────┐
│ 6. 응답 반환            │
│ - JSON 직렬화          │
│ - 캐싱 헤더 추가       │
└────────────────────────┘
```

---

## 성능 최적화 전략

### 1. 매칭 캐싱

```typescript
// Redis 캐시 (예정)
const cacheKey = `match:${bid.id}`;
const cached = await redis.get(cacheKey);

if (cached) {
  return JSON.parse(cached);
}

const result = matchBidToProducts(bid);
await redis.setex(cacheKey, 3600, JSON.stringify(result)); // 1시간 캐싱
return result;
```

### 2. 배치 처리

```typescript
// 여러 공고를 한 번에 매칭
async function batchMatch(bids: BidAnnouncement[]): Promise<MatchResult[]> {
  return Promise.all(bids.map(bid => matchBidToProducts(bid)));
}
```

### 3. 인덱싱 전략

```sql
-- 자주 조회되는 컬럼에 인덱스
CREATE INDEX idx_bids_deadline ON bids(deadline);
CREATE INDEX idx_bids_source ON bids(source);
CREATE INDEX idx_matches_score ON matches(score DESC);
CREATE INDEX idx_matches_confidence ON matches(confidence);
```

---

## 에러 처리 및 폴백

### 매칭 실패 시

```typescript
try {
  const result = matchBidToProducts(bid);
  return result;
} catch (error) {
  logger.error('Matching failed', { bidId: bid.id, error });

  // 폴백: 기본 매칭 결과 반환
  return {
    bestMatch: null,
    allMatches: [],
    recommendation: 'REVIEW',
    error: '자동 매칭 실패 - 수동 검토 필요'
  };
}
```

---

## 참고 자료

- **Enhanced Matcher 소스**: `src/lib/matching/enhanced-matcher.ts`
- **제품 데이터**: `src/lib/data/products.ts`
- **샘플 매칭 결과**: `src/lib/data/mock-bids.ts`

---

**마지막 업데이트**: 2025-12-21
**문서 버전**: 1.0.0
**작성자**: BIDFLOW 개발팀
